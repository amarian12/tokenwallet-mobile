import { MnemonicExistDirective } from './mnemonic-exist.directive';

describe('MnemonicExistDirective', () => {
  it('should create an instance', () => {
    const directive = new MnemonicExistDirective();
    expect(directive).toBeTruthy();
  });
});
