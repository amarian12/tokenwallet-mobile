import { MnemonicValidDirective } from './mnemonic-valid.directive';

describe('MnemonicValidDirective', () => {
  it('should create an instance', () => {
    const directive = new MnemonicValidDirective();
    expect(directive).toBeTruthy();
  });
});
